services:
  web:
    build:
      context: .
    image: agentic-small-app-web:latest
    container_name: agentic-small-app-web
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
